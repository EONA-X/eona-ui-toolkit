<!-- Generated by `cargo xtask bridgegen` from the eona-ui-toolkit Yew crate. Do not edit. -->

# @eona-x/ui-toolkit-react

React components for the EONA-X design system. **This package is generated.** The source
of truth is the Yew crate in this repository (`src/`): each component here was produced by
rendering the Rust component with Yew's own server renderer and splicing the props back
into the resulting markup, so the React and Yew renders are the same HTML by construction.

Generated from src_hash `e2f87d37a98628ba1179b6685c343ac908f0107ef8e37da01f0b07580c98f5f9`.

## Do not edit

Every file under `src/` is overwritten by the next `cargo xtask bridgegen`, and `cargo xtask check`
re-runs the generator and diffs the result, so a hand edit is reported rather than
surviving quietly. (It is a command, not yet a pipeline: the crate has no CI
configuration — eona-x/backlog#808.)
To change a component, change the Rust component it came from — the `file:line` is
in the header of every generated file — and regenerate.

## Install

**Not published yet.** Packaging and publishing `@eona-x/ui-toolkit-react` is Layer 4 of
eona-x/backlog#809. Until it lands, copy `packages/react/src/*.tsx` into the app, or
vendor this repository and run `npm install && npm run build` in `packages/react` first —
every entry point in `package.json` is under `dist/`, which is generated and not committed.

```sh
npm install @eona-x/ui-toolkit-react
```

`react >= 19` is a peer dependency: `NavDropdown` renders `inert`
(`src/molecules/nav_dropdown.rs:29`), which React only added to its DOM attribute types
in 19.

The markup carries the design system's class names and is unstyled on its own. There is
no `@eona-x/ui-toolkit-css` package yet, so this package declares **no runtime dependency**.
Do not import the Yew crate's `assets/components.css` whole into an app meanwhile: it
opens with a page-shell reset on `*`, `html`, `body`, `img`, `a`, `h1`-`h4`, `p` and
`code` (`assets/components.css:13-30`) and sets `html > body { padding-top: 156px }` at
`:393`, so it resets the host's typography and displaces its layout. The published
`@eona-x/ui-toolkit-css` drops those rules and scopes the rest under `.eona-ui`, which the markup
will then need as an ancestor (eona-x/backlog#808 Layer 3). `tokens.css` is safe as-is:
it declares nothing but `:root` custom properties.

```tsx
import { Button } from '@eona-x/ui-toolkit-react';

export default function Page() {
  return <Button label="Get started" href="/docs" />;
}
```

One component needs a file this package does not carry: `SiteHeader` hardcodes
`logo/logo-transparent.svg` (`src/organisms/site_header.rs:101,105`) and must find it at
that path relative to the page, or it renders a broken image where the EONA-X mark goes.

`RuleListItem` needs an ancestor rather than an asset: every rule that styles it is
scoped under `.rules-list`, so render it inside a `<ul className="rules-list">`.

## Server components

Every component is presentational and holds no state, so none of them carries a
client-component directive — the string does not occur anywhere in the package — and all
of them render inside a React Server Component. The
toolkit's stateful ontology browser is not here to port: it lives in `eona-vocabulary-ui`,
which owns it outright. Anything browser-only belongs in a hand-written client component.

## SSR output is equivalent, not byte-identical

The elements, attributes and class names match the Yew render. The *serialisation* does
not, in four measured ways, none of which changes what a browser shows. Do not write a
snapshot test that diffs this package's SSR bytes against the Yew portal's:

1. **Trailing `;` in `style`.** Yew emits the author's string verbatim
   (`style="position:relative;"`, `src/molecules/nav_dropdown.rs:24`); React serialises a
   style *object* and writes no trailing semicolon.
2. **Whitespace in `style`.** React normalises `background :  #fff ` to `background:#fff`.
3. **Whitespace in `class`.** Yew's `Classes` collapses runs of whitespace; React does not,
   so a prop containing a newline reaches the class attribute intact
   (`src/atoms/code_block.rs:38` interpolates `language`).
4. **React's own additions.** React 19 hoists a `<link rel="preload" as="image">` for
   every `<img src>` it renders, and omits `src=""` entirely (with a warning) where Yew
   emits it.

## Components (41)

- `AccordionItem` — `src/molecules/accordion_item.rs:17`
- `BadgeNum` — `src/atoms/badge_num.rs:11`
- `BgCheckItem` — `src/molecules/bg_check_item.rs:16`
- `Button` — `src/atoms/button.rs:30`
- `Callout` — `src/molecules/callout.rs:13`
- `CaseCard` — `src/molecules/case_card.rs:15`
- `CodeBlock` — `src/atoms/code_block.rs:27`
- `DatasetCard` — `src/molecules/dataset_card.rs:57`
- `DeclCard` — `src/molecules/decl_card.rs:17`
- `EvolutionBadge` — `src/atoms/evolution_badge.rs:23`
- `Hero` — `src/organisms/hero.rs:35`
- `HeroFilter` — `src/organisms/hero_filter.rs:34`
- `LogoDownloadCard` — `src/molecules/logo_download_card.rs:22`
- `MainNav` — `src/organisms/main_nav.rs:26`
- `MemberCard` — `src/molecules/member_card.rs:11`
- `MemberFilterCard` — `src/molecules/member_filter_card.rs:18`
- `MetricTile` — `src/molecules/metric_tile.rs:28`
- `Modal` — `src/molecules/modal.rs:28`
- `MvoCard` — `src/molecules/mvo_card.rs:11`
- `NavDropdown` — `src/molecules/nav_dropdown.rs:22`
- `NewsCard` — `src/molecules/news_card.rs:16`
- `OntoAnnotation` — `src/molecules/annotation.rs:38`
- `OntoBadge` — `src/atoms/onto_badge.rs:66`
- `PaletteGroup` — `src/molecules/palette_group.rs:43`
- `PersonalityCard` — `src/molecules/personality_card.rs:11`
- `Pill` — `src/atoms/pill.rs:28`
- `PillButton` — `src/atoms/pill_button.rs:37`
- `PillarCard` — `src/molecules/pillar_card.rs:13`
- `PropsTable` — `src/molecules/props_table.rs:40`
- `RefCard` — `src/molecules/ref_card.rs:14`
- `RuleListItem` — `src/molecules/rule_list_item.rs:28`
- `ScoreDonut` — `src/molecules/score_donut.rs:29`
- `SectionHead` — `src/molecules/section_head.rs:15`
- `ShapeCard` — `src/molecules/shape_card.rs:12`
- `SimpleFooter` — `src/organisms/simple_footer.rs:13`
- `SiteHeader` — `src/organisms/site_header.rs:96`
- `Swatch` — `src/molecules/swatch.rs:44`
- `TagLabel` — `src/atoms/tag_label.rs:27`
- `TextField` — `src/molecules/text_field.rs:22`
- `TypeScaleRow` — `src/molecules/type_scale_row.rs:14`
- `UsageExample` — `src/molecules/usage_example.rs:24`

## Out of scope (4)

Recorded here because a component that is missing on purpose and one that is missing
by accident look identical from the outside.

- **crate showcase page** (1): DemoPage
- **zero-prop demo section** (3): ComponentsGallery, IntegrationSection, OntologySection
